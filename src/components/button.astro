---
type Props = {
  className?: string;
};

const { className = "" } = Astro.props;
---

<button
  class={`${className} py-6 w-full text-white bg-[#2B2A26] rounded-[50px] lg:max-w-[360px] lg:text-xl transition hover:bg-[#4A4842]`}
>
  Отримати доступ
</button>
